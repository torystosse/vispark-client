import React from 'react'

// note: this file will be used in future iterations. This will be the landing
// page before a user clicks on a link for all parks.

const Home = (props) => (
  <div>
    <p>Check out all the U.S. parks on our app, and see where you want to go next!</p>
  </div>
)

export default Home
